import { type VoidComponent } from "solid-js";

const TagsCreatePage: VoidComponent = () => {
  return (
    <section class="container">
      <h1>tags create</h1>
    </section>
  );
};

export default TagsCreatePage;
